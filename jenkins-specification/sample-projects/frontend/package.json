{
  "name": "jenkins-sample-frontend",
  "version": "1.0.0",
  "description": "Sample frontend project for Jenkins CI/CD",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "build": "node build.js",
    "test": "jest --coverage",
    "lint": "eslint src/",
    "docker:build": "docker build -t frontend:latest .",
    "docker:push": "docker push frontend:latest"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "eslint": "^8.50.0"
  }
}